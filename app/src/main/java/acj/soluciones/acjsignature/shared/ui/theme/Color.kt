package acj.soluciones.acjsignature.shared.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary ──────────────────────────────────────────────────────────────
val DeepPurple = Color(0xFF2C0745)
val Magenta = Color(0xFFA500A3)
val MagentaLight = Color(0xFFCB1BC8)
val PinkLight = Color(0xFFFFD7F5)
val PinkSoft = Color(0xFFF3DAFF)
val PinkOverlay = Color(0x33FFD7F5) // 20% alpha

// ── Surfaces ─────────────────────────────────────────────────────────────
val SurfaceBg = Color(0xFFF9F9F9)
val CardBg = Color(0xFFF5F5F5)
val CardBg2 = Color(0xFFF3F3F3)
val White = Color(0xFFFFFFFF)

// ── Text / Content ───────────────────────────────────────────────────────
val TextPrimary = Color(0xFF2C0745)
val TextBody = Color(0xFF544150)
val TextMuted = Color(0xFF877081)
val TextOnDark = Color(0xFFFFFFFF)
val TextOnDarkMuted = Color(0xCCFFD7F5) // 80% alpha

// ── Semantic ─────────────────────────────────────────────────────────────
val GreenActive = Color(0xFF08F070)
val GreenDark = Color(0xFF047A39)
val GreenOverlay = Color(0x1A08F070)     // 10% green bg
val GreenBorder = Color(0x3308F070)      // 20% green border
val YellowWarning = Color(0xFFFFF9DB)
val YellowWarningText = Color(0xFF92400E)
val Error = Color(0xFFBA1A1A)

// ── Borders / Dividers ───────────────────────────────────────────────────
val BorderLight = Color(0x26D9BFD1)      // 15% (#D9BFD1)
val BorderMedium = Color(0x4DD9BFD1)     // 30%
val BorderDashed = Color(0xFFD9BFD1)
val DividerLight = Color(0xFFE8E8E8)

// ── Gradient references (used in Brush) ──────────────────────────────────
val GradientMagentaStart = Magenta
val GradientMagentaEnd = MagentaLight
val GradientDarkStart = DeepPurple
val GradientDarkEnd = Color(0xFF2E0A47)
